<p>
  <strong>Sub Task:</strong>
  <%= sub_task.text %>
  <%= sub_task.complete ? '✅' : '❌' %>
</p>

<p>
  <%= link_to 'Show', task_sub_task_path(@task, sub_task) %>
  <%= link_to 'Delete', [sub_task.task, sub_task],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
</p>